import React from 'react';
import ReactDOM from 'react-dom/client';
import App from './Styled Component/part_2';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
    <App />
);